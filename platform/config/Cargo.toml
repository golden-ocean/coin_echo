[package]
name = "platform_config"
version = "0.1.0"
edition = "2024"

[dependencies]
# 内部
platform_kernel = { workspace = true }

# 序列化
serde = { workspace = true }
serde_json = { workspace = true }
# 错误处理
thiserror = { workspace = true }
# 日志
# tracing = { workspace = true }
# 配置
# figment = { version = "0.10", features = ["env"] }
config = { version = "0.15" }
dotenvy = { version = "0.15" }

[dev-dependencies]
